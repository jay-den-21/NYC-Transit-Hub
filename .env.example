# MTA_API_KEY is optional; leave blank unless the feeds require a key again.
MTA_API_KEY=
# Port the backend listens on
PORT=4000

# Database (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=NYC_transit
